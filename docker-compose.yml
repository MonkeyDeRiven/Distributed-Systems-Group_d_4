version: '3'


volumes:
  reportportal-data:
    # external: true

services:
  ubuntu:
    image: ubuntu
    ## Uncomment if needed
    # ports:
    #   - "6969:6969"
    volumes:
      - reportportal-data:/data
    restart: always